# Desvíos respecto a `frontend_spec.md`

El spec es normativo: la estructura, medidas, proporciones, breakpoints y animaciones se replican
al milímetro. Este documento registra **todo** lo que se aparta de él, con su motivo, para que la
revisión de aceptación pueda comprobar cada caso en vez de descubrirlo.

Regla general: si un desvío no está aquí, es un bug.

| #    | Desvío | Motivo | Estado |
| ---- | ------ | ------ | ------ |
| D-0  | Tokens de color re-tematizados a la marca. `--primary` se divide en `--gold` (relleno), `--gold-display` (texto ≥24px), `--gold-ink` (texto <24px), `--gold-line` (trazos) y `--gold-bright` (decorativo sobre oscuro). La etiqueta de `.btn` pasa de blanca a `--text-dark`. `--body-text` de `#707070` a `#6B5B4D`. Crema de `#f5f0ec` a `#FAF5EC`. | Decisión de marca + AA. Blanco sobre `#E8A81B` da **2.09:1**, así que `.btn{background:primary; color:white}` es literalmente inviable. La solución la dicta el propio logo: marrón sobre amarillo. Blindado en `tests/unit/contrast.test.ts`. | ✅ Fase 0 |
| D-1  | No se cargan los pesos 300 y 600 de Cormorant Infant ni el 300 de Libre Franklin. Lato no se carga en la portada. | Ninguna regla del spec los referencia. Lato lo usa un único selector (`blockquote`) y no hay blockquote en la portada. Ahorra ~5 archivos woff2. | ✅ Fase 0 |
| D-12 | Las tres `font-family` literales del spec pasan a variables CSS (`--ff-display`, `--ff-sans`, `--ff-quote`). | `next/font` no permite fijar el nombre de familia. A cambio se autohospedan las fuentes: sin petición a Google, sin render-block de terceros, sin FOUT de red. | ✅ Fase 0 |
| D-39 | El nombre «Boquita» del hero usa los contornos exactos del SVG maestro y no Cormorant Infant en mayúsculas. | El logotipo es lettering propio: una fuente aproximada altera la `B`, la `q`, la inclinación y los remates. El h1 conserva «Boquita» como texto accesible y presenta el arte vectorial blanco como imagen decorativa. | ✅ Marca |
| D-6  | El eyebrow `h6.h6-sans` se renderiza como `<p class="h6-sans">`. | Un `h6` antes de un `h2` rompe el orden de encabezados y axe lo marca como violación. Pixel-idéntico: `.h6-sans` ya redefine fuente, tamaño, peso, letter-spacing y transform. | ✅ Fase 0 |
| D-7  | El `h4` de la newsletter se renderiza como `<h2 class="as-h4">`. | El último encabezado del documento es un `h2`; pasar a `h4` salta un nivel. Pixel-idéntico. | ✅ Fase 0 |
| D-8  | El input de la newsletter recibe un `<label>` visualmente oculto. | Un placeholder no es una etiqueta accesible. El markup del spec sólo tiene placeholder. | ✅ Fase 0 |
| D-2  | Se añade un botón «Añadir +» en la fila de `.menu-item-tag`, que pasa a `justify-content:space-between`. | Requisito del carrito. Deja `.menu-item-head` (nombre + precio, `gap:10px`) **byte-idéntico** al spec, que es lo que inspecciona el punto 5 del checklist. | ⏳ Fase 4 |
| D-3  | `will-change:transform` se mueve de `.track` a `.track.is-animating`. | El spec lo pone permanente. Con 14 imágenes serían dos capas compositadas grandes siempre activas, caro en móvil. Invisible y estrictamente mejor. | ⏳ Fase 1 |
| D-4  | Se añade un scrim `.nav-scrim` a ≤991px. | Un panel de 320px sobre contenido vivo no tiene afordancia de cierre ni separación visual. No está en el spec. | ⏳ Fase 1 |
| D-5  | A ≤991px, `.navbar` recibe `background:rgba(255,255,255,.92)` + `backdrop-filter:blur(6px)`. | El navbar es `position:absolute; background:transparent`, y a ≤991 `.hero-img` pasa a ser una foto estática justo debajo: el logo, la hamburguesa y el carrito quedarían sobre la fotografía con contraste desconocido. Necesario para los puntos 13 y 14 del checklist. | ⏳ Fase 1 |
| D-9  | ~~`<div class="hero-img">` envolviendo un `<Image fill>`~~ | **Descartado.** Las 15 imágenes del layout son assets de marca fijos, con recortes ya pre-generados por `scripts/build-images.mjs`. Servirlas con `<img srcset>` plano evita el optimizador de Next por completo (cero cuota de transformaciones, CDN inmutable, comportamiento idéntico en preview y producción) y **el hero conserva el `<img>` del spec sin wrapper**. El `preload` con `fetchpriority="high"` se declara a mano en el `<head>`, y el AVIF se negocia con `<picture>`. `next/image` se reserva para las fotos de producto alojadas en Blob (Fase 4), donde sí hace falta un ladder dinámico. | ❌ innecesario |
| D-10 | `.media` deja de ser `background-image` y pasa a un `<img srcset>` dentro de un `.media` con `position:relative; overflow:hidden`. | Un background CSS no admite `srcset` ni lazy loading, y es una foto de 493×300 que se enviaría a tamaño completo a un móvil de 390px. Se conservan `height:300px` y el centrado flex. | ⏳ Fase 1 |
| D-11 | `.hero{height:100vh}` → `100svh` con fallback `100vh`. | **Condicional**: sólo si el salto de la barra de direcciones de Safari en iOS se juzga inaceptable en el pase de dispositivo real. En escritorio son idénticos. | ⏳ Fase 6 |

## Descubiertos al verificar la Fase 1

Todos éstos salieron de ejecutar la verificación de geometría a los 8 anchos y de
mirar capturas reales. Ninguno se habría visto leyendo el CSS.

| #    | Desvío | Motivo | Estado |
| ---- | ------ | ------ | ------ |
| D-14 | `picture { display: contents }` en el reset | **Imprescindible, no cosmético.** El CSS del spec asume que `.hero-img` es hijo directo de `.hero`, y usamos `<picture>` para ofrecer AVIF. Con el `<picture>` generando caja, el `width:100%` de la imagen a ≤991px se resolvía contra ella —que se dimensiona por contenido— y la foto del hero salía a **315px en vez de 991px**. | ✅ Fase 1 |
| D-15 | `.menu-grid` usa `minmax(0, 1.2fr) minmax(0, 1fr)` en vez de `1.2fr 1fr` | `1fr` es en realidad `minmax(auto, 1fr)`: si el min-content de una columna supera su parte, se la queda. Con los nombres en español las columnas salían **270/380px, o sea 0.71:1**, invirtiendo la jerarquía que exige el punto 5 del checklist. `minmax(0, …)` devuelve el mando a las fracciones. | ✅ Fase 1 |
| D-16 | `.menu-item-head { flex-wrap: wrap }` y el precio con `flex:none` | El diseño de referencia usaba nombres cortos («MARGHERITA»); en español hay algunos de 30 caracteres. Sin esto, el precio se partía por dentro («₡ 14.000 / CRC»), que se lee como un error. Ahora baja entero a la línea siguiente. | ✅ Fase 1 |
| D-17 | El precio del catálogo se muestra sin el sufijo «CRC» | El spec §8 pide **adaptar** el formato a la moneda del proyecto. Con «CRC» el precio no cabía junto al nombre en ninguna columna, y esa misma línea es la firma visual de la rejilla. En un sitio costarricense el símbolo ₡ no deja lugar a dudas. `formatCRC` (con sufijo) se conserva para contextos donde la moneda sí deba ser explícita. | ✅ Fase 1 |
| D-18 | `.media .play-wrap { margin: 0 }` + sombra en los anillos del play | Dos problemas juntos. (1) El spec le da `height:100%` con `margin-top:35px`, lo que dentro del `.media` de 300px con el `overflow:hidden` que introduce D-10 desplazaba el botón 35px bajo el centro y lo recortaba. (2) Los anillos son blancos y el spec asume una foto oscura; la foto disponible es clara y el botón desaparecía. Una sombra los hace visibles sobre cualquier imagen sin cambiar el diseño. | ✅ Fase 1 |
| D-20 | La imagen del hero **no** lleva reveal, aunque el spec §4.1 la incluya en la lista | Dos razones que se refuerzan. (1) Es el elemento LCP: cualquier `opacity:0` inicial lo aplazaría hasta la hidratación. (2) Envolverla en un div reintroduce el bug de D-14 — a ≤991 la imagen es `width:100%` y se resolvería contra el envoltorio en vez de contra `.hero`, saliendo a 315px. Animar la portada no vale ninguna de las dos cosas. | ✅ Fase 1 |
| D-21 | Las flechas del slider usan `aria-disabled`, no `disabled` | Al deshabilitar un elemento que **tiene el foco**, el navegador lo manda al `<body>`. Con `disabled`, un usuario de teclado que llegara al último slide con `End` perdía el foco y las teclas siguientes ya no alcanzaban al carrusel. Con `aria-disabled` el botón sigue enfocable, el lector de pantalla anuncia que no está disponible, y el handler no hace nada. | ✅ Fase 1 |
| D-22 | Con hover activo, el clic en un dropdown del nav **abre** en lugar de alternar | Todo clic de ratón va precedido de un `mouseenter`. Si el clic alternara, el `mouseenter` abriría el panel y el clic lo cerraría acto seguido: el usuario ve el menú colapsarse justo al pulsar la etiqueta. Con hover, cerrar es tarea de `mouseleave`, Escape o un clic fuera; en táctil (sin hover) el clic sí alterna. | ✅ Fase 1 |
| D-19 | `.newsletter-form .input { flex: none }` a ≤479px | **Corrige un conflicto latente del propio spec.** `.input{flex:1}` está pensado para la fila; al pasar a `flex-direction:column` el eje principal es el vertical y `flex-basis:0` gana sobre `height:60px`, así que el input **colapsaba a 21px**. El punto 10 del checklist exige 60px. | ✅ Fase 1 |
| D-23 | La rejilla del catálogo de la portada puede renderizar **menos de 8** productos, contra el punto 5 del checklist | Sólo ocurre si la tabla `products` tiene entre 1 y 7 filas: con la base ausente, vacía o caída se sirve el fallback de 14 y son 8 exactos. La alternativa era rellenar con productos del fallback que no estén en el catálogo servido, y sus tarjetas llevarían a un 404 porque `/tienda/[slug]` resuelve contra el mismo `getCatalog()`. Una rejilla de 6 celdas es un defecto de maquetación; una tarjeta que lleva a un 404 le cuesta un pedido. Se avisa por consola y hay tests que fijan las dos ramas. | ✅ Fase 2 |
| D-24 | El nav ya no usa megamenú de productos; `Catálogo` es enlace a `/tienda` con dropdown de categorías | El flujo real de compra vive en `/tienda`, con filtros y búsqueda. Duplicar los 14 productos en el header hacía ruido y competía con el destino principal. El dropdown queda como ayuda contextual: categorías, sin guiones decorativos y con hover/focus visible. | ✅ Header v1 |
| D-25 | El footer visible cambia de CTA/newsletter solapada a cierre editorial de 4 columnas | La newsletter todavía no tiene backend ni contrato legal final; mostrar un formulario `action="#"` era ruido. El cierre actual prioriza marca, navegación, dirección y contacto real. | ✅ Home v2 |
| D-26 | En el statement, el texto **aún no revelado** (`--text-ghost`) se queda en 1.53:1 sobre el blanco de la sección, por debajo del 3:1 que le tocaría como texto grande. Y desde «Titular alineado» el titular revela a `--gold-display`, con lo que su salto baja a 2.61× y su estado final a AA-*large* (3.99:1) en vez del AA normal que daba `--gold-ink` | Es el estado transitorio de una animación, y subirlo la mataba. El cambio de token alinea el titular con los demás `h2` y con la regla de ámbar por tamaño. Detalle abajo. | ✅ Home v2 · revisado en Titular alineado |
| D-27 | El hover de los enlaces del nav no es sólo color: es `background:--primary-light` + `color:--gold-ink` sobre una caja con `border-radius:4px`. El spec §4 lo define como cambio de `color` a `--primary` en `.3s`, y en los enlaces de dropdown como un deslizamiento de `margin-left:-20px → 0`. El tamaño baja además de 20px a 18px | Un cambio de color a secas no da afordancia de fila pulsable, y el `margin-left` animado desplaza el texto bajo el cursor. El par fondo+tinta es el patrón que ya usa todo el sitio (`.cart-button`, `.qty-button`, `.shop-filter`). Los 18px mantienen `--gold-ink` en el carril de 4.5:1 (ver «Umbral de contraste» abajo). Estaba sin registrar desde Header v1 (mencionado de refilón en D-24). | ✅ Header v1 |
| D-28 | El drawer móvil adapta `sidebar-nav-spec.md` a la marca: superficie `--surface-dark` en vez de `#031C30`, crema/blanco y dorado en vez de azul/gris, ancho `min(325px,100vw)` y subítems de **44px** en vez de 40. Se omiten marca interna, tema, logout y controles duplicados | El encargo exige conservar los colores y la funcionalidad de Boquita. Los 44px mantienen el contrato táctil anterior; limitar el ancho evita desbordar pantallas menores que los 325px de Figma. Añadir tema/logout inventaría acciones y duplicar búsqueda o carrito rompería el árbol único actual. La geometría, Poppins, iconos, slots, radios y alineación de texto a 64px sí proceden del kit. Lo miden `geometry.spec.ts` e `interactions.spec.ts` a 991/767/479/390. | ✅ Sidebar móvil |
| D-29 | En el drawer, hover/foco/desplegado usan fondos translúcidos chocolate/dorado y barra interior de 3px, sin desplazar ni escalar etiquetas. El hover sólo existe con puntero fino; `:active` cubre el tacto. `prefers-reduced-motion` elimina entrada lateral, fundido del scrim y giro del chevron | El movimiento de texto anterior competía con los iconos y alteraba la alineación exacta a 64px. Fondo, tinta y barra dan feedback sin reflow; el modo táctil no retiene un `:hover` pegado. El kill-switch conserva el cambio de estado pero elimina el recorrido espacial. El cierre pasa a 48×48px y hereda los mismos estados. | ✅ Sidebar móvil |
| D-40 | El buscador de productos **no desaparece a ≤991**, contra el `@media (max-width:991px){ .nav-search{ display:none } }` del spec §5, y desde el 14 ago va en la **misma fila** que el logo, la cesta y la hamburguesa: `.logo` baja de 72 a **42px** (46 con el `padding` que 03-base.css da a todo `a`, la altura exacta del buscador y de la cesta), los tres huecos entre controles miden **12px** —8 a ≤479— y `--header-h` pasa de 155 a **71px**. El `.nav-menu-wrapper` se saca de flujo con `position:absolute` y `.brand` pierde su `margin-right:auto`. | Esconder el catálogo tras la hamburguesa en el dispositivo donde más se compra es enterrar la conversión del sitio; el spec §5 nunca contempló que la tienda fuera el destino principal. La segunda fila que lo resolvía costaba **155px de cabecera fija** —un cuarto de la primera pantalla de un teléfono— y obligaba a montar `ProductSearchAutocomplete` dos veces, con una copia siempre en `display:none`: de ahí salía el `.nav-search-input` duplicado que rompía por modo estricto los localizadores CSS de los tests. Los huecos iguales los pone un único `gap` repetido en `.nav-container` y `.navbar-actions`; lo miden `geometry.spec.ts` («los cuatro controles del header movil comparten linea y distancia» y «el header monta un unico buscador») a los cuatro anchos móviles. | ✅ Cabecera |

## D-26: el estado sin revelar del statement

El `ScrollColorText` de la portada pinta cada palabra dos veces —el fantasma debajo y el color
revelado encima, recortado por el scroll—. En Fase 1 el fantasma estaba en 1.36:1 y la tarea UI-060
lo subió a 3.31:1 para cumplir AA-large. Cumplía el umbral y **mataba el efecto**: al acercarlo tanto
al color final, el salto quedó en 1.65× en el titular y 1.80× en el cuerpo. En pantalla se lee como
que no pasa nada, que fue exactamente el reporte del cliente.

El umbral estaba mirando la mitad equivocada del problema: medía fantasma contra fondo, cuando lo que
decide si el efecto existe es fantasma contra revelado. Se vuelve a `rgba(58,42,26,.22)`.

**Se mide sobre BLANCO, no sobre crema.** `.statement-section` tiene `background: var(--white)`
(`styles/12-statement.css:9`, afirmado por `geometry.spec.ts:113`); el crema es el bloque del
catálogo, que va debajo y es otra sección. Como `--text-ghost` es translúcido, el fondo decide su
color real: sobre blanco aplana a `#d4d0cd`, sobre crema a `#cfc8be`. El test estuvo aplanándolo
contra el crema —un color que no aparece en pantalla en ningún momento— y por eso publicaba saltos
un ~8% cortos. Corregido en «Titular alineado»; las cifras de aquí abajo son ya las del fondo real.

**El titular revela a `--gold-display`, no a `--gold-ink`.** Es un `h2` de 50/42/34px, o sea el
carril de ≥24px, y era el único titular del sitio pintado con el token de <24px: se veía apagado
junto a los demás y el cliente lo reportó como «otra tipografía» —no lo era, la familia, el peso y
el tamaño ya coincidían—. El cambio cuesta salto, y se aceptó a sabiendas: **2.61×** en vez de los
3.87× que daba `--gold-ink`. El cuerpo no cambia de token y se queda en **4.24×**.

Lo que sostiene el desvío:

- **El estado final sí cumple**: `--gold-display` da 3.99:1 y `--body-text` 6.50:1 sobre blanco. El
  titular cumple AA-*large*, que es el umbral que le toca por tamaño; el cuerpo, AA normal. Antes el
  titular cumplía AA normal de sobra (5.92:1), y ese margen es justo lo que se gastó en alinearlo.
- **Con `prefers-reduced-motion: reduce` no existe el estado intermedio**: todo nace revelado, tanto
  por CSS (`12-statement.css`) como por la rama de JS del componente.
- **Sin JavaScript tampoco existe**: `--reveal` vale `100%` por defecto y es la clase `.js` quien lo
  baja a `0%`.
- **Es transitorio por construcción**: el revelado se completa dentro de un scroll de una altura de
  viewport, y el texto queda en su color final mientras se lee.

Lo vigila `tests/unit/contrast.test.ts`, que ya **no** comprueba el umbral absoluto del fantasma sino
el salto entre los dos estados más el cumplimiento AA del estado final. Los mínimos son **2.5× en el
titular y 3.5× en el cuerpo**, y la asimetría no es una relajación: con `--gold-display` el techo
absoluto son **3.99×** —el salto que daría un fantasma blanco puro, o sea invisible—, así que 3.5×
es inalcanzable por construcción. 2.5× sigue muy por encima del 1.65× con el que UI-060 mató el
efecto, que es el número que decide si el usuario ve algo. Si alguien vuelve a oscurecer el fantasma
«para cumplir mejor», el build cae ahí — que es lo que no ocurrió la primera vez.

| D-30 | La ficha de producto añade un **selector de presentación** (`<fieldset class="variant-picker">` con radios) entre la descripción y el stepper de cantidad, y el precio pasa a renderizarse en el cliente. El spec §6.4 y §7 sólo contemplan un precio y una unidad de venta fijos por producto | El catálogo real de Ale trae **60 presentaciones para 23 productos**: el queque de zanahoria va a ₡2.500 pequeño, ₡12.000 mediano y ₡24.000 grande. Sin selector no hay forma de pedir un tamaño concreto, que es la decisión de compra. Son radios y no un `<select>` porque los tres precios tienen que verse a la vez para poder compararlos, y un desplegable los esconde justo cuando importan. El precio vive en el mismo estado que el selector porque depende de él; la descripción se le pasa como `children` para que siga siendo del servidor y el orden visual de la ficha no cambie. Con una sola presentación el fieldset no se dibuja. | ✅ Catálogo de Ale |
| D-31 | La tarjeta del catálogo anuncia **«desde ₡ X»** con el precio de la presentación más barata, y su tercera línea pasa de `categoría · unidad` a `categoría · subcategoría · todas las presentaciones` | Con varias presentaciones no hay un precio único que anunciar. Se elige el mínimo con «desde» —lo que ya significaba el flag `priceFrom`— porque es lo que no engaña: publicar el del tamaño mediano haría que la tarjeta prometiera menos de lo que cuesta lo que la mayoría pide, y publicar el máximo espantaría. Consecuencia visible: el queque de zanahoria pasa de «₡ 14.000» a «desde ₡ 2.500». La línea de presentaciones se compacta en `lib/variants.ts` (`pequeño, mediano o grande`, `6, 12 o 24 unidades`) porque con los paréntesis del Excel no cabe en una línea. | ✅ Catálogo de Ale |
| D-32 | La ficha del **queque personalizado** muestra dos fotos, una bajo la otra. El spec da una sola foto por producto | Es el único producto que se vende por encargo: ver dos encargos distintos —un queque de zanahoria de dos pisos de cumpleaños y uno de chocolate de graduación— explica qué se compra mejor que cualquier párrafo. Es un campo opcional (`imageB`) y hoy sólo lo usa este producto. | ✅ Catálogo de Ale |
| D-33 | La tarjeta del catálogo pasa de foto + texto centrado sobre blanco a **tarjeta con superficie**: fondo `--primary-light` en la zona de texto, foto **a sangre** recortada por un envoltorio nuevo `.shop-card-media` con `overflow: hidden`, texto **alineado a la izquierda** (se cae el `text-align: center`), y una fila nueva `.shop-card-foot` anclada al fondo con el precio a la izquierda y un enlace **«Pedir»** a la derecha. El orden pasa de foto → nombre → precio → descripción → etiquetas a **foto → nombre → descripción → etiquetas → precio + Pedir**. La foto escala a `1.06` al pasar el ratón por cualquier parte de la tarjeta. Se añade la variante `.btn--sm` (16px, `padding: 10px 20px`, 46px de alto) y el carril lateral de 20px lo pone cada elemento de texto, no la tarjeta. El JSX se extrae a `components/shop/ProductCard.tsx`, compartido por `/tienda` y la 404 | El spec **no especifica `/tienda`**: no define su rejilla ni su tarjeta, y su checklist §9 no tiene ni un punto sobre esta página; la única tarjeta que sí describe es la de reseñas (§6.7). Tres motivos. **(1)** Sin CTA el único destino pulsable era el nombre: 24px de alto para 23 productos, y la acción que el catálogo existe para provocar no tenía verbo en ninguna parte. Es **«Pedir»** y no «Añadir al carrito» porque el queque personalizado se cotiza y **no entra al carrito** — el verbo nombra la intención y no el mecanismo, y así sirve para los 23; no es UI-063, que sigue abierta. **(2)** El fondo crema y la alineación izquierda son lo que separa una tarjeta de una tira de fotos sueltas: con todo centrado sobre blanco las tres columnas se leían como una sola mancha. **(3)** El acercamiento va en `transform` sobre la foto y no en `width` ni `padding` —sin reflow, sin arrastrar el texto—, envuelto en `@media (hover: hover) and (pointer: fine)` con `:focus-visible` fuera, igual que D-29, y enumerado a mano en el kill-switch de `99-a11y.css`. Se conserva el `border-radius: 0` de las tarjetas; los 5px del botón son del spec §3.1. Ningún nombre de clase existente se renombra: son el contrato de `tienda.spec.ts`. El precio sigue en `--gold-ink` (5.45:1 sobre crema) y el botón es relleno `--gold` con etiqueta `--on-gold` (6.58:1), como manda D-0. El carril lateral no puede ir en `.shop-card` porque la foto dejaría de medir los **370px** con los que se calcularon `CARD_SIZES` y la escalera `[400, 800, 1200]`. Cuesta un tab stop extra por tarjeta —46 en `/tienda`—, que es el precio de tener foto y CTA pulsables sin anidar interactivos; el CTA lleva `aria-label="Pedir {producto}"` para que la lista de enlaces de un lector no sean 23 «Pedir» idénticos. | ✅ Catálogo v2 |
| D-35 | La sección **statement no es la del spec §6.2**, y nunca lo estuvo registrado. El spec pide un `h2` centrado a todo el ancho con **exactamente dos `.inline-img`** incrustadas en el flujo del texto (`frontend_spec.md:355-379`), y el punto del checklist §9 que exige esos dos recortes queda **derogado**. Lo que hay es una rejilla de dos columnas (`.statement-media-layout`, 0.92fr/1fr) con la foto a la izquierda y, a la derecha, `h2` + `p` cuyo color barre el scroll palabra a palabra. Encima, tres cosas que el spec no contempla: **(a)** a ≥992 la columna de texto se estira a la altura de la foto (`align-items: stretch` + `justify-content: space-between`) para que el titular arranque en el borde superior de la foto y el párrafo acabe en el inferior; **(b)** ese ras es **óptico**, con el medio-espacio de línea recortado a mano (`margin-top: -0.18em` en el `h2`, y el del `p` recalculado con el interlineado que toque); **(c)** el barrido de color acaba 80px **antes** del borde inferior de la cabecera (`--header-h` + `HEADER_LEAD`), no en el borde superior del viewport; **(d)** a ≥992 el cuerpo **no mide 18px**: va a `clamp(22px, 1.8vw, 26px)`, y su **interlineado lo calcula JavaScript** (`fitBody()`) repartiendo la banda entre las líneas reales, con `--body-lh`/`--body-trim` y fallback en CSS | El §6.2 describe una banda decorativa sin contenido: el análisis de la referencia lo confirma («**0 imágenes**, 0 botones, 0 enlaces», `ANALISIS-REFERENCIA…:254-274`) y el propio spec se contradice con él al exigir dos recortes. Esta sección tiene un trabajo distinto y concreto: **es el único sitio de la portada donde se cuenta quién es Ale**, con su nombre, su cantón y su año. Eso es texto que se lee, no un rótulo, y necesita una foto que lo respalde — de ahí la rejilla. Lo demás son consecuencias de haberlo medido en pantalla: **(a)** con `align-items: center` el titular quedaba a media altura de la foto y el párrafo flotando, y las dos columnas no se leían como un bloque; **(b)** `space-between` alinea **cajas de línea** y el ojo alinea **trazos** — sin recortar, Cormorant Infant a `line-height: 1em` deja el titular 9px por debajo del borde de la foto y Libre Franklin a 1.8em deja la última línea 9,7px corta. Los dos números están **medidos** con canvas TextMetrics sobre las webfonts cargadas, no estimados, y se recorta hasta cap-height y línea base —no hasta el trazo real— porque son constantes de la fuente y no bailan según qué glifos caigan en la línea; **(c)** la cabecera es `position: fixed` y tapa 101-115px: terminando en `y=0` el texto se metía debajo con el color a medias, que fue el reporte del cliente. El recorte óptico vive bajo `@media (min-width: 992px)` y no más abajo por dos razones que se suman: apilado no hay bordes de foto contra los que alinear, y el valor del `p` depende del `line-height`, que a ≤991 baja a 1.7em. **(d)** es el precio de (a): con el titular arriba y el párrafo abajo, `space-between` metía TODA la holgura en un agujero central. Con 18px el copy son 3 líneas (97px) contra una banda de 158-215px, o sea hasta **133px de hueco** entre titular y texto — el segundo reporte del cliente, y con razón: se lee como un fallo de maquetación. Para llenarla hay que crecer, y el copy es fijo, así que crecen tamaño e interlineado. **El tamaño es fluido porque 26px fijos NO caben:** medido, a 992-1024 la banda son 158-168px y 26px pide 6 líneas, con lo que el reparto deja el interlineado en 0,91-0,97em, por debajo de `ascent+descent` — líneas solapadas. 22px es el **único** tamaño que da 4 líneas en todo el rango de dos columnas (medido a 1920/1600/1440/1280/1100/1024/992), así que 26px donde cabe y 22px donde no. **Y el interlineado no puede ser un número:** con uno fijo el error va de −55px a +50px según el ancho porque el número de líneas salta, y `content/schema.ts` admite un `body` de 40 a 280 caracteres —hoy tiene 165—, así que cualquier valor a mano caduca en cuanto Ale cambie el texto. `fitBody()` cuenta las líneas reales agrupando las palabras por su borde superior y despeja `LH = [H − h2Outer − marginTop + (desc − asc)/2] / (N − 0,5)`; el `−0,5` sale de que el recorte de abajo depende del propio interlineado. Se reajusta tras `document.fonts.ready` —con `display: swap` el primer cálculo iría contra system-ui— y en `resize`, **nunca en el scroll**. Contrapartidas asumidas: se rompe la regla más explícita de la referencia («el tamaño del párrafo no cambia nunca», `ANALISIS…:1256`) y el relleno **exacto** depende de JS — sin él queda el fallback de 1.6em, que llena de sobra y mantiene la línea base a ras, sólo con más hueco arriba (66px medidos a 1440). El interlineado se acota a 1,15-2,8em y, cuando un tope actúa, `fitBody()` lo marca en `data-fit` del `<p>`: un tope silencioso se leería como «llena bien». Lo fija `geometry.spec.ts`, que mide el **trazo** y no la caja —una aserción sobre cajas pasaría igual con el recorte mal puesto—, más el hueco titular↔párrafo y que `data-fit` no exista. **Titular y cuerpo van centrados a todos los anchos** (antes: titular a la izquierda y cuerpo justificado a ≥992). Eso **acerca** la sección a la referencia, que describe el H-02 como «un único H2 en mayúsculas, **centrado**» (`ANALISIS…:262`), y de paso quita el justificado, que con una columna estrecha abría ríos entre palabras. El `text-align` deja de repetirse en el bloque de ≤991: centrar es ahora la base de la sección, y tenerlo en dos sitios dejaba que el de abajo tapara en silencio un cambio del de arriba. D-26 sigue cubriendo el contraste del estado sin revelar. | ✅ Statement v3 |
| D-34 | La tarjeta de reseña **no lleva retrato**. El spec §6.7 la define con un `.review-avatar` de **70×70 cuadrado sin redondear** (`frontend_spec.md:653`, `:691`, `:904`) a la izquierda del par nombre + rol; aquí `.review-head` sólo contiene el nombre y el rol, apilados. Con el retrato fuera, `.review-head` **deja de ser flex**: sus dos hijos pasaron a ser hijos directos y en flex se colocarían en fila. Se borra la regla `.review-avatar` y el componente `components/ui/Avatar.tsx`, que era su único consumidor | **No hay fotos de clientes reales y no las va a haber en v1.** Ilustrar reseñas de un negocio que existe con caras de stock de personas que no existen es una falsedad sobre el propio contenido, no una licencia estética; ya estaba descartado en `ESTADO.md` («lo que no se va a hacer»). El sustituto anterior era un SVG con las iniciales, y se retira también: ocupaba los 70px de una foto y no aportaba lo que una foto aporta —cara, contexto, prueba de que hay alguien detrás—, así que era ruido con la forma de un dato. Sin él la jerarquía la llevan la tipografía y el espacio en blanco, que es exactamente como la referencia dice construirla («diseño plano, sin sombras; la jerarquía se construye con tipografía, color y espacio en blanco»). Los `min-height` de `.review-card` **no cambian**: el retrato y el par nombre + rol iban lado a lado con `align-items: flex-start`, así que quitarlo resta ~13px de contenido, no 70. Lo fija `geometry.spec.ts`, que ahora afirma `.review-avatar` count 0 y que las tarjetas a la vista cuadran de altura | ✅ Testimonios |
| D-36 | `/sobre-nosotros` **no usa el ritmo vertical de `.section`**. El §2.5 da un `padding-block` de 80px para toda sección, 120 a ≥1280 y 60 a ≤767 (`styles/04-layout.css`); esta página lo sustituye por un hueco **único de 64px, 48 a ≤767** (`--editorial-rhythm` en `styles/40-prose.css`), igual en los cinco cortes verticales de la página y **sin crecer a ≥1280**. De paso recorta a 0 cuatro márgenes de la tipografía base en los cantos —el del eyebrow, el del último párrafo de la entradilla, y el del primer `.prose-block` con el de su `h2`—, y la raya decorativa bajo cada titular pasa de **56px a la medida completa del carril** (1170px), lo que deja sin sentido el `border-top` de la lista de preguntas frecuentes, que se retira | Los huecos salían de **tres sitios que no se hablaban entre sí** —el `padding-block` de `.section`, el `margin-top: 64px` de `.prose-block` y los `margin: 10px 0` de `03-base.css`— y sumados daban **cinco huecos distintos** en una página que se lee de arriba abajo: a ≥1280, 130px bajo la cabecera, 130 hasta la rueda, **184** de la rueda al primer titular, 64 entre bloques y 120 hasta el pie. Los 184 no se leen como aire, se leen como un fallo de maquetación, y es lo que reportó el cliente junto al blanco de la primera pantalla. **El 64 no está inventado: es el ritmo que la prosa ya tenía**, así que adoptarlo hace que todo lo demás baje hasta él y que **nada crezca** — que es justo lo que se pidió; 80 uniforme habría cumplido la igualdad pero a 992-1279 sólo bajaría 10px el hueco de la cabecera y a ≤767 lo **subiría**. **No sube a ≥1280** porque el 80→120 de `.section` existe para una portada de bandas full-bleed cuya imaginería escala con la ventana, y aquí no escala nada: el carril está topado en 1170px y la escala tipográfica está congelada por encima de 992 (h1 70, h2 50, cuerpo 19/1,75), o sea que a 1920 los elementos son idénticos a los de 1100 y un hueco un 50% mayor entre elementos idénticos es sólo un agujero mayor. Los cuatro recortes de margen hacen falta porque `.container` es flex —sus márgenes no colapsan contra el padding de la sección— y `.prose` es flex item —contexto propio, del que el margen del primer bloque no se escapa—: sin ellos quedan residuos de 10px que nadie ve a ojo. Van por **posición** (`:first-child`/`:last-child`) y no por clase para que no caduquen si cambia el markup. La raya completa es la otra mitad del mismo reporte: a 56px era una marca junto al titular, y con ocho secciones a 1170px de ancho lo que hace legible la jerarquía es que cada titular **cierre su banda**; sigue siendo decorativa y va DEBAJO del titular, así que no mueve el ancla ni su `scroll-margin-top`, y `--gold-line` sigue dando 4.00:1 ≥ 3:1 no-textual. Dos trampas quedan escritas en el CSS: se escribe `--section-pad` y **nunca** el atajo `padding-block` —desde `.editorial-page` sería (0,2,0) y le ganaría a `.section--no-bottom` (0,1,0) por especificidad, no por orden, devolviendo el aire fantasma que documenta `04-layout.css`—, y esa misma especificidad tapa los dos overrides responsivos de `.section`, así que el valor de móvil vive en `40-prose.css` o no existe. Lo fija `geometry.spec.ts` («todos los huecos verticales miden lo mismo» y «la raya del titular cubre el carril»), que afirma la aritmética término a término para saber qué margen volvió y no sólo que el total no cuadra | ✅ Sobre Nosotros |

| D-37 | `/aviso-legal` deja la medida acotada de 720px y adopta el carril editorial completo, el ritmo 64/48px, el cuerpo 19/1,75 (18/1,7 en móvil) y la raya de 2px bajo cada `h2` de `/sobre-nosotros`. La cabecera editorial y el cuerpo pasan a dos `.section` consecutivas bajo `.editorial-page` | El cliente pidió que ambos bloques de texto tuvieran **exactamente** el mismo `max-width`, alineación, márgenes y espaciado. Compartir la base en vez de duplicarla conserva los 1170px útiles del contenedor en desktop y los 15px laterales en móvil. La escala y el interlineado compensan la línea ancha; los seis divisores permiten escanear el contenido sin añadir tarjetas, índice ni colores nuevos. El 14 de agosto el cliente retiró expresamente la frase fiscal provisional y la sección «Servicios de terceros»; no se altera el resto del copy. `geometry.spec.ts` compara cajas reales entre ambas páginas, verifica el ritmo, la tipografía, las rayas y el `scrollWidth` en los ocho anchos; `paginas.spec.ts` conserva contenido y `noindex`, y axe sigue en verde | ✅ Aviso Legal |

| D-38 | La portada deja de sumar el padding inferior de una sección y el superior de la siguiente: todos los cortes visuales usan un único `--home-section-gap` —120px a ≥1715px, fluido en escritorio medio y 60px a ≤767—. En el cambio de fondo catálogo→servicio se reparte a medias entre crema y blanco; Service y Gallery anulan el lado que duplicaría el siguiente hueco. El último corte vive completo al final de Testimonials para formar una banda blanca antes del footer; el padding marrón posterior sigue siendo interior del pie | El cliente señaló el corte catálogo→servicio: a ≥1280 sumaba **170px** al final del bloque crema y hasta **112px** al inicio de Service, es decir **282px**, más del doble que los 120px del ejemplo aprobado alrededor del statement. La misma suma reaparecía entre Service→Galería (64+120). Unificar cada transición visual —no cada padding aislado— mantiene el aire del ejemplo ideal y evita que el color de fondo o el tipo de sección alteren la cadencia. En el último corte el cliente pidió expresamente que la separación fuese **blanca**, no parte del fondo marrón: por eso se mide hasta el borde del footer y no hasta sus columnas. `geometry.spec.ts` compara cajas reales en las seis transiciones de Home y pasa a los ocho anchos; las capturas de portada se revisaron a 1920 y 390px | ✅ Ritmo Home |

## No son desvíos

Cosas que parecen apartarse del spec pero no lo hacen:

- **`.mt-40` vale 30px, no 40px.** Es una rareza del original y se copia literal
  (`styles/04-layout.css`). Cambiarla desplazaría los botones del hero.
- ~~**`.inline-img` sigue siendo `background-image`**, no `next/image`.~~ **Obsoleto: `.inline-img` ya
  no se renderiza en ninguna parte** — el statement dejó de ser el `h2` con dos recortes del spec §6.2.
  Ver **D-35**, y `content.test.ts`, que afirma que el copy no contiene `inline`.
- **Los `%` del transform de `.track` se resuelven contra la caja propia** (100% de `.scroller`),
  no contra la tira de contenido que desborda a ~161%. Eso es lo que hacía el original: no se
  «arregla» a px.
- **El `218px` de `.slider-arrow--left` se copia, no se recalcula.** Es `160 + 34 + 24`.
- **Los avatares de reseñas son SVG con iniciales**, no fotografías. No hay caras de clientes
  reales disponibles e inventar personas con fotos de stock sería deshonesto para un negocio real.
  Se respeta el `70×70` cuadrado sin redondear que pide el spec §7.

## Umbral de contraste: WCAG, no el del spec

El checklist §9 punto 13 dice «`#cb6037` sobre blanco sólo en textos ≥18px o bold». WCAG 2.x es más
estricto: «large» es **≥24px, o ≥18.66px en negrita**. Se aplica el umbral real de WCAG.

Consecuencia práctica: `h2` (50/42/34px), `h4` (30/26px), `.text-primary` (86…46px) y `.stat-num`
(50/42/38/36px) sí entran en el carril de 3:1 y usan `--gold-display`. Los precios (18/20px),
nav/search/cart (18px), `a:hover` (20px), `li` (16px), `.h6-sans.primary` (20px), los iconos
sociales y el glifo de las flechas del slider **no**, y usan `--gold-ink` (4.5:1).
